<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%# messages() %>
            <% if (errors) { %>
                <ul class="notice">
                    <% errors.array().forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>

                    <div class="vehicle-card">
                        <%- card %>
                    </div>

                    <div class="bottom-section">
                        <div class="input-review">
                            <form action="/inv/review" method="post">
                                <h3> Leave review for the Car</h3>
                                <input type="hidden" name="inv_id" value="<%= inv_id %>">
                                <input type="hidden" name="account_id" value="<%= account_id %>">

                                <label for="review_rating">Rating (1 to 5)</label>
                                <select name="review_rating" id="review_rating" required>
                                    <option value="">Select a Rating</option>
                                    <option value="1">1 - Negative</option>
                                    <option value="2">2 - Not Great</option>
                                    <option value="3">3 - Decent</option>
                                    <option value="4">4 - Great</option>
                                    <option value="5">5 - Amazing</option>
                                </select>

                                <label for="review_text">Review:</label>
                                <label for="review_text">Vehicle Description:</label>
                                <input type="text" id="review_text" name="review_text" required>

                                <button type="submit">Submit Review</button>
                            </form>
                        </div>
                        <div class="output-review">
                            <h3>Reviews</h3>
                            <% reviews.forEach(review=> { %>
                                <div class="review">
                                    <p>
                                        Reviewer: <%= review.account_firstname %>
                                    </p>
                                    <p>Rating: <%= review.review_rating %> / 5</p>
                                    <p>Description:
                                        <%= review.review_text %>
                                    </p>
                                    <p>Reviewed Date <%= new Date(review.review_date).toLocaleDateString() %>
                                    </p>
                                </div>
                                <% }) %>
                        </div>
                    </div>